language: node_js
node_js:
  - "stable"

cache:
  directories:
    - "node_modules"

install:
  - npm install

script:
  - npm test

after_script: 
  - "cat coverage/lcov.info | node_modules/coveralls/bin/coveralls.js"

after_success:
  - npm run coverage

